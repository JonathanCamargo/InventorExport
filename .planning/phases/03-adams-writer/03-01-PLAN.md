---
phase: 03-adams-writer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inventor_exporter/writers/__init__.py
  - src/inventor_exporter/writers/protocol.py
  - src/inventor_exporter/writers/registry.py
autonomous: true

must_haves:
  truths:
    - "FormatWriter Protocol can be implemented without inheritance"
    - "WriterRegistry.register decorator registers writer classes"
    - "WriterRegistry.get returns writer class by format name"
    - "WriterRegistry.list_formats returns available format names"
    - "get_writer() convenience function returns instantiated writer"
  artifacts:
    - path: "src/inventor_exporter/writers/protocol.py"
      provides: "FormatWriter Protocol definition"
      exports: ["FormatWriter"]
      min_lines: 40
    - path: "src/inventor_exporter/writers/registry.py"
      provides: "WriterRegistry with decorator registration"
      exports: ["WriterRegistry"]
      min_lines: 50
    - path: "src/inventor_exporter/writers/__init__.py"
      provides: "Package public API"
      exports: ["FormatWriter", "WriterRegistry", "get_writer"]
  key_links:
    - from: "src/inventor_exporter/writers/__init__.py"
      to: "src/inventor_exporter/writers/protocol.py"
      via: "imports FormatWriter"
      pattern: "from.*protocol import FormatWriter"
    - from: "src/inventor_exporter/writers/__init__.py"
      to: "src/inventor_exporter/writers/registry.py"
      via: "imports WriterRegistry"
      pattern: "from.*registry import WriterRegistry"
---

<objective>
Create the writer infrastructure: FormatWriter Protocol and WriterRegistry.

Purpose: This establishes the plugin architecture that allows adding new export formats without modifying core code. The Protocol defines what writers must implement; the Registry provides discovery and lookup.

Output:
- `src/inventor_exporter/writers/protocol.py` - FormatWriter Protocol
- `src/inventor_exporter/writers/registry.py` - WriterRegistry class
- `src/inventor_exporter/writers/__init__.py` - Package exports
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-adams-writer/03-RESEARCH.md
@src/inventor_exporter/model/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FormatWriter Protocol</name>
  <files>src/inventor_exporter/writers/protocol.py</files>
  <action>
Create FormatWriter Protocol using typing.Protocol with @runtime_checkable:

1. Import from typing: Protocol, runtime_checkable
2. Import from pathlib: Path
3. Use TYPE_CHECKING guard for AssemblyModel import (avoid circular deps)

4. Define FormatWriter Protocol with:
   - format_name property (str) - unique identifier like "adams"
   - file_extension property (str) - extension with dot like ".cmd"
   - write(model: AssemblyModel, output_path: Path) -> None method

5. Add comprehensive docstrings explaining:
   - This is structural subtyping (no inheritance needed)
   - Example implementation pattern
   - Expected behavior of write() method (validate, convert, generate, write)
   - What exceptions write() should raise (ValueError for validation, IOError for file errors)

Use the exact pattern from 03-RESEARCH.md "Complete FormatWriter Protocol" section.
  </action>
  <verify>
python -c "from inventor_exporter.writers.protocol import FormatWriter; print(FormatWriter)"
  </verify>
  <done>
FormatWriter Protocol imports successfully and is runtime_checkable
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WriterRegistry with decorator registration</name>
  <files>src/inventor_exporter/writers/registry.py</files>
  <action>
Create WriterRegistry class for writer discovery and lookup:

1. Import from typing: Dict, Type, Optional, List
2. Import logging
3. Import FormatWriter from protocol module

4. Create WriterRegistry class with class-level _writers dict:
   - _writers: Dict[str, Type[FormatWriter]] = {}

5. Implement class methods:
   - register(format_name: str) - decorator that adds writer class to registry
     - Convert format_name to lowercase for case-insensitive lookup
     - Log warning if overwriting existing registration
     - Log debug message on successful registration
   - get(format_name: str) -> Optional[Type[FormatWriter]] - lookup by name
   - list_formats() -> List[str] - return sorted list of registered names
   - get_or_raise(format_name: str) -> Type[FormatWriter] - lookup or raise KeyError

6. In get_or_raise, if format not found:
   - List available formats in error message
   - Use format: "Unknown format 'xyz'. Available formats: adams, urdf"

Use the exact pattern from 03-RESEARCH.md "Complete WriterRegistry" section.
  </action>
  <verify>
python -c "from inventor_exporter.writers.registry import WriterRegistry; print(WriterRegistry.list_formats())"
  </verify>
  <done>
WriterRegistry class imports and list_formats() returns empty list (no writers registered yet)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create writers package __init__.py</name>
  <files>src/inventor_exporter/writers/__init__.py</files>
  <action>
Create package __init__.py that:

1. Add module docstring explaining:
   - This package provides format writers for exporting AssemblyModel
   - Lists the public API: FormatWriter, WriterRegistry, get_writer
   - Notes that built-in writers are auto-registered on import

2. Import and re-export:
   - FormatWriter from protocol
   - WriterRegistry from registry

3. Create get_writer(format_name: str) -> FormatWriter convenience function:
   - Call WriterRegistry.get_or_raise(format_name) to get class
   - Instantiate and return the writer instance
   - Add docstring with Args, Returns, Raises

4. NOTE: Do NOT import adams module yet (it doesn't exist). That import will be added in Plan 02 when AdamsWriter is created.

5. Define __all__ = ["FormatWriter", "WriterRegistry", "get_writer"]
  </action>
  <verify>
python -c "from inventor_exporter.writers import FormatWriter, WriterRegistry, get_writer; print('Imports OK')"
  </verify>
  <done>
Package exports FormatWriter, WriterRegistry, and get_writer. get_writer raises KeyError for unknown formats.
  </done>
</task>

</tasks>

<verification>
1. All three files exist and are importable:
   ```
   python -c "from inventor_exporter.writers import FormatWriter, WriterRegistry, get_writer"
   ```

2. FormatWriter is a Protocol and runtime_checkable:
   ```
   python -c "from inventor_exporter.writers import FormatWriter; print(hasattr(FormatWriter, '_is_protocol'))"
   ```

3. WriterRegistry registration decorator works:
   ```
   python -c "
   from inventor_exporter.writers import FormatWriter, WriterRegistry
   from pathlib import Path

   @WriterRegistry.register('test')
   class TestWriter:
       format_name = 'test'
       file_extension = '.txt'
       def write(self, model, path): pass

   print('test' in WriterRegistry.list_formats())
   print(WriterRegistry.get('test') is TestWriter)
   "
   ```

4. get_writer raises KeyError for unknown format:
   ```
   python -c "
   from inventor_exporter.writers import get_writer
   try:
       get_writer('nonexistent')
   except KeyError as e:
       print('KeyError raised:', 'nonexistent' in str(e))
   "
   ```
</verification>

<success_criteria>
- WRITER-01: FormatWriter Protocol defines interface for all format writers
- WRITER-02 (partial): WriterRegistry can register and lookup writers by format name
- No writers registered yet (that's Plan 02)
- All imports work without circular dependency errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-adams-writer/03-01-SUMMARY.md`
</output>
