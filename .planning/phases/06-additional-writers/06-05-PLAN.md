---
phase: 06-additional-writers
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - tests/writers/test_urdf.py
  - tests/writers/test_sdf.py
  - tests/writers/test_mujoco.py
  - tests/writers/test_mesh_converter.py
autonomous: true

must_haves:
  truths:
    - "URDF writer tests verify XML structure and coordinate conventions"
    - "SDF writer tests verify XML structure and coordinate conventions"
    - "MuJoCo writer tests verify XML structure and quaternion handling"
    - "Mesh converter tests verify STEP to STL conversion"
    - "All four new writers appear in --list-formats output"
  artifacts:
    - path: "tests/writers/test_urdf.py"
      provides: "URDF writer test coverage"
      min_lines: 100
    - path: "tests/writers/test_sdf.py"
      provides: "SDF writer test coverage"
      min_lines: 100
    - path: "tests/writers/test_mujoco.py"
      provides: "MuJoCo writer test coverage"
      min_lines: 100
    - path: "tests/writers/test_mesh_converter.py"
      provides: "Mesh converter test coverage"
      min_lines: 50
  key_links:
    - from: "tests/writers/test_urdf.py"
      to: "src/inventor_exporter/writers/urdf.py"
      via: "import and instantiation"
      pattern: "from inventor_exporter\\.writers import"
    - from: "tests/writers/test_sdf.py"
      to: "src/inventor_exporter/writers/sdf.py"
      via: "import and instantiation"
      pattern: "from inventor_exporter\\.writers import"
    - from: "tests/writers/test_mujoco.py"
      to: "src/inventor_exporter/writers/mujoco.py"
      via: "import and instantiation"
      pattern: "from inventor_exporter\\.writers import"
---

<objective>
Create comprehensive test suites for all Phase 6 writers and mesh converter.

Purpose: Verify that URDF, SDF, and MuJoCo writers generate correct XML with proper coordinate conventions. Verify mesh converter handles STEP-to-STL conversion.

Output: Four test files covering all new functionality with registration, output structure, coordinate conversion, and edge case tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-additional-writers/06-CONTEXT.md

# Existing test patterns
@tests/writers/test_adams.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create URDF writer tests</name>
  <files>tests/writers/test_urdf.py</files>
  <action>
Create test file following test_adams.py structure.

Test classes:
1. TestURDFWriterRegistration
   - test_urdf_writer_registered: "urdf" in WriterRegistry.list_formats()
   - test_urdf_writer_properties: format_name="urdf", file_extension=".urdf"

2. TestURDFWriterOutput
   - test_write_simple_assembly: writes valid XML file
   - test_output_has_robot_root: root element is `<robot name="...">`
   - test_output_has_base_link: contains `<link name="base_link"/>`
   - test_output_has_fixed_joints: contains `<joint type="fixed">`
   - test_materials_defined_once: material elements at robot level

3. TestURDFWriterConversions
   - test_position_in_meters: position values match IR (no conversion)
   - test_rotation_as_rpy_radians: rotation as RPY, values in radians
   - test_inertia_values: inertia tensor in kg*m^2

4. TestURDFWriterValidation
   - test_validation_error_raised: invalid model raises ValueError

5. TestURDFWriterGeometry
   - test_mesh_reference: mesh filename in meshes/ subdirectory
   - test_visual_and_collision: both visual and collision elements present

Use fixtures from test_adams.py (simple_material, simple_transform, simple_body, simple_assembly).

Parse XML output with lxml.etree to verify structure:
```python
from lxml import etree
tree = etree.parse(str(output_path))
root = tree.getroot()
assert root.tag == "robot"
```
  </action>
  <verify>`pytest tests/writers/test_urdf.py -v` passes with all tests</verify>
  <done>test_urdf.py exists with 10+ tests covering registration, output structure, conversions, validation, and geometry</done>
</task>

<task type="auto">
  <name>Task 2: Create SDF and MuJoCo writer tests</name>
  <files>tests/writers/test_sdf.py, tests/writers/test_mujoco.py</files>
  <action>
Create test files following same pattern as test_urdf.py.

**test_sdf.py:**
1. TestSDFWriterRegistration
   - test_sdf_writer_registered
   - test_sdf_writer_properties (format_name="sdf", file_extension=".sdf")

2. TestSDFWriterOutput
   - test_write_simple_assembly
   - test_output_has_sdf_root: root is `<sdf version="1.8">`
   - test_output_has_model: contains `<model>` element
   - test_output_has_base_link
   - test_pose_element: uses `<pose>` with 6 values

3. TestSDFWriterConversions
   - test_position_in_meters
   - test_rotation_as_rpy_radians
   - test_inertia_values

4. TestSDFWriterGeometry
   - test_mesh_uri: mesh referenced via `<uri>` element

**test_mujoco.py:**
1. TestMuJoCoWriterRegistration
   - test_mujoco_writer_registered
   - test_mujoco_writer_properties (format_name="mujoco", file_extension=".xml")

2. TestMuJoCoWriterOutput
   - test_write_simple_assembly
   - test_output_has_mujoco_root: root is `<mujoco model="...">`
   - test_output_has_asset_section: contains `<asset>` element
   - test_output_has_worldbody: contains `<worldbody>` element
   - test_compiler_meshdir: contains `<compiler meshdir="meshes"/>`

3. TestMuJoCoWriterConversions
   - test_position_in_meters
   - test_rotation_as_quaternion: quat attribute with 4 values
   - test_quaternion_format: w x y z order (scalar first)

4. TestMuJoCoWriterGeometry
   - test_mesh_asset_defined: mesh in asset section
   - test_geom_references_mesh: geom uses mesh="..." attribute

Parse XML with lxml for all tests.
  </action>
  <verify>
1. `pytest tests/writers/test_sdf.py -v` passes
2. `pytest tests/writers/test_mujoco.py -v` passes
  </verify>
  <done>
test_sdf.py exists with 10+ tests
test_mujoco.py exists with 10+ tests
  </done>
</task>

<task type="auto">
  <name>Task 3: Create mesh converter tests</name>
  <files>tests/writers/test_mesh_converter.py</files>
  <action>
Create test file for mesh_converter module.

Test classes:
1. TestMeshConverterImport
   - test_module_imports: module can be imported
   - test_functions_exported: convert_step_to_stl and MeshConverter available

2. TestMeshConverterClass
   - test_init_creates_output_dir: MeshConverter(tmp_path) creates meshes/ subdir
   - test_get_mesh_path: returns relative path from output_dir
   - test_convert_returns_relative_path: convert() returns Path in meshes/

3. TestCadQueryAvailability
   - test_cadquery_unavailable_error: if cadquery missing, raises clear RuntimeError

Note: Full conversion tests require actual STEP files which may not be available in test environment. Tests should:
- Verify interface works
- Verify error handling for missing cadquery
- Mock cadquery for path management tests if needed

Use pytest.importorskip("cadquery") for tests that require actual conversion.
  </action>
  <verify>`pytest tests/writers/test_mesh_converter.py -v` passes</verify>
  <done>test_mesh_converter.py exists with 5+ tests covering import, class interface, and error handling</done>
</task>

</tasks>

<verification>
1. All new tests pass: `pytest tests/writers/test_urdf.py tests/writers/test_sdf.py tests/writers/test_mujoco.py tests/writers/test_mesh_converter.py -v`
2. All existing tests still pass: `pytest tests/ -v`
3. Total test count increased: `pytest tests/ --collect-only | grep "test session starts" -A 5`
</verification>

<success_criteria>
- test_urdf.py: 10+ tests for URDF writer
- test_sdf.py: 10+ tests for SDF writer
- test_mujoco.py: 10+ tests for MuJoCo writer
- test_mesh_converter.py: 5+ tests for mesh converter
- All 78+ existing tests still pass
- Total test count ~115+ (78 existing + ~37 new)
</success_criteria>

<output>
After completion, create `.planning/phases/06-additional-writers/06-05-SUMMARY.md`
</output>
